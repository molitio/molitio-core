import { ThemeColor } from "@molitio/system-schema";

export interface TypographyProps {
    rem: number;
    color?: ThemeColor;
    marginTop?: string;
    maxContent?: boolean;
    center?: boolean;
    textContent?: string;
}
